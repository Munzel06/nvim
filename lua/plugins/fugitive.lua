-- ~/.config/nvim/lua/plugins/fugitive.lua
return {
  {
    'tpope/vim-fugitive',
    -- No specific config needed from your example, but you can add keymaps or settings here
    -- cmd = "Git", -- Optionally, make it load on :Git command
  }
}
